# Dependencies are declared in pyproject.toml; this file just forwards to it
# so `pip install -r requirements.txt` keeps working. The core package needs
# only numpy -- the encoder, the benchmarks and the ANN backends are extras.
#
# For the demo, tests and benchmarks:
#   pip install -e ".[encoder,dev,bench]"
# Plus approximate nearest neighbour backends and the FastAPI example:
#   pip install -e ".[ann,serve]"
-e .
